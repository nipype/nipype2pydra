# This file is used to manually specify the semi-automatic conversion of
# 'nipype.interfaces.brainsuite.brainsuite.Hemisplit' from Nipype to Pydra.
#
# Please fill-in/edit the fields below where appropriate
#
# Docs
# ----
# 
#     Hemisphere splitter
#     Splits a surface object into two separate surfaces given an input label volume.
#     Each vertex is labeled left or right based on the labels being odd (left) or even (right).
#     The largest contour on the split surface is then found and used as the separation between left and right.
# 
#     Examples
#     --------
# 
#     >>> from nipype.interfaces import brainsuite
#     >>> from nipype.testing import example_data
#     >>> hemisplit = brainsuite.Hemisplit()
#     >>> hemisplit.inputs.inputSurfaceFile = 'input_surf.dfs'
#     >>> hemisplit.inputs.inputHemisphereLabelFile = 'label.nii'
#     >>> hemisplit.inputs.pialSurfaceFile = 'pial.dfs'
#     >>> results = hemisplit.run() #doctest: +SKIP
# 
#     
task_name: Hemisplit
nipype_name: Hemisplit
nipype_module: nipype.interfaces.brainsuite.brainsuite
inputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    inputSurfaceFile: generic/file
    # type=file|default=<undefined>: input surface
    inputHemisphereLabelFile: generic/file
    # type=file|default=<undefined>: input hemisphere label volume
    pialSurfaceFile: generic/file
    # type=file|default=<undefined>: pial surface file -- must have same geometry as input surface
  metadata:
  # dict[str, dict[str, any]] - additional metadata to set on any of the input fields (e.g. out_file: position: 1)
outputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    outputLeftHemisphere: generic/file
    # type=file: path/name of left hemisphere
    # type=file|default=<undefined>: output surface file, left hemisphere. If unspecified, output file name will be auto generated.
    outputRightHemisphere: generic/file
    # type=file: path/name of right hemisphere
    # type=file|default=<undefined>: output surface file, right hemisphere. If unspecified, output file name will be auto generated.
    outputLeftPialHemisphere: generic/file
    # type=file: path/name of left pial hemisphere
    # type=file|default=<undefined>: output pial surface file, left hemisphere. If unspecified, output file name will be auto generated.
    outputRightPialHemisphere: generic/file
    # type=file: path/name of right pial hemisphere
    # type=file|default=<undefined>: output pial surface file, right hemisphere. If unspecified, output file name will be auto generated.
  callables:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set to the `callable` attribute of output fields
  templates:
  # dict[str, str] - `output_file_template` values to be provided to output fields
    outputLeftHemisphere: outputLeftHemisphere
    # type=file: path/name of left hemisphere
    # type=file|default=<undefined>: output surface file, left hemisphere. If unspecified, output file name will be auto generated.
    outputRightHemisphere: outputRightHemisphere
    # type=file: path/name of right hemisphere
    # type=file|default=<undefined>: output surface file, right hemisphere. If unspecified, output file name will be auto generated.
    outputLeftPialHemisphere: outputLeftPialHemisphere
    # type=file: path/name of left pial hemisphere
    # type=file|default=<undefined>: output pial surface file, left hemisphere. If unspecified, output file name will be auto generated.
    outputRightPialHemisphere: outputRightPialHemisphere
    # type=file: path/name of right pial hemisphere
    # type=file|default=<undefined>: output pial surface file, right hemisphere. If unspecified, output file name will be auto generated.
  requirements:
  # dict[str, list[str]] - input fields that are required to be provided for the output field to be present
tests:
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    inputSurfaceFile:
    # type=file|default=<undefined>: input surface
    inputHemisphereLabelFile:
    # type=file|default=<undefined>: input hemisphere label volume
    outputLeftHemisphere:
    # type=file: path/name of left hemisphere
    # type=file|default=<undefined>: output surface file, left hemisphere. If unspecified, output file name will be auto generated.
    outputRightHemisphere:
    # type=file: path/name of right hemisphere
    # type=file|default=<undefined>: output surface file, right hemisphere. If unspecified, output file name will be auto generated.
    pialSurfaceFile:
    # type=file|default=<undefined>: pial surface file -- must have same geometry as input surface
    outputLeftPialHemisphere:
    # type=file: path/name of left pial hemisphere
    # type=file|default=<undefined>: output pial surface file, left hemisphere. If unspecified, output file name will be auto generated.
    outputRightPialHemisphere:
    # type=file: path/name of right pial hemisphere
    # type=file|default=<undefined>: output pial surface file, right hemisphere. If unspecified, output file name will be auto generated.
    verbosity:
    # type=int|default=0: verbosity (0 = silent)
    timer:
    # type=bool|default=False: timing function
    args:
    # type=str|default='': Additional parameters to the command
    environ:
    # type=dict|default={}: Environment variables
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
doctests: []
