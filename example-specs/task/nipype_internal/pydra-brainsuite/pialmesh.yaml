# This file is used to manually specify the semi-automatic conversion of
# 'nipype.interfaces.brainsuite.brainsuite.Pialmesh' from Nipype to Pydra.
#
# Please fill-in/edit the fields below where appropriate
#
# Docs
# ----
# 
#     pialmesh
#     computes a pial surface model using an inner WM/GM mesh and a tissue fraction map.
# 
#     http://brainsuite.org/processing/surfaceextraction/pial/
# 
#     Examples
#     --------
# 
#     >>> from nipype.interfaces import brainsuite
#     >>> from nipype.testing import example_data
#     >>> pialmesh = brainsuite.Pialmesh()
#     >>> pialmesh.inputs.inputSurfaceFile = 'input_mesh.dfs'
#     >>> pialmesh.inputs.inputTissueFractionFile = 'frac_file.nii.gz'
#     >>> pialmesh.inputs.inputMaskFile = example_data('mask.nii')
#     >>> results = pialmesh.run() #doctest: +SKIP
# 
#     
task_name: Pialmesh
nipype_name: Pialmesh
nipype_module: nipype.interfaces.brainsuite.brainsuite
inputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    inputSurfaceFile: generic/file
    # type=file|default=<undefined>: input file
    inputTissueFractionFile: generic/file
    # type=file|default=<undefined>: floating point (32) tissue fraction image
    inputMaskFile: generic/file
    # type=file|default=<undefined>: restrict growth to mask file region
  metadata:
  # dict[str, dict[str, any]] - additional metadata to set on any of the input fields (e.g. out_file: position: 1)
outputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    outputSurfaceFile: generic/file
    # type=file: path/name of surface file
    # type=file|default=<undefined>: output file. If unspecified, output file name will be auto generated.
  callables:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set to the `callable` attribute of output fields
  templates:
  # dict[str, str] - `output_file_template` values to be provided to output fields
    outputSurfaceFile: outputSurfaceFile
    # type=file: path/name of surface file
    # type=file|default=<undefined>: output file. If unspecified, output file name will be auto generated.
  requirements:
  # dict[str, list[str]] - input fields that are required to be provided for the output field to be present
tests:
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    inputSurfaceFile:
    # type=file|default=<undefined>: input file
    outputSurfaceFile:
    # type=file: path/name of surface file
    # type=file|default=<undefined>: output file. If unspecified, output file name will be auto generated.
    verbosity:
    # type=int|default=0: verbosity
    inputTissueFractionFile:
    # type=file|default=<undefined>: floating point (32) tissue fraction image
    numIterations:
    # type=int|default=100: number of iterations
    searchRadius:
    # type=float|default=1: search radius
    stepSize:
    # type=float|default=0.4: step size
    inputMaskFile:
    # type=file|default=<undefined>: restrict growth to mask file region
    maxThickness:
    # type=float|default=20: maximum allowed tissue thickness
    tissueThreshold:
    # type=float|default=1.05: tissue threshold
    outputInterval:
    # type=int|default=10: output interval
    exportPrefix:
    # type=str|default='': prefix for exporting surfaces if interval is set
    laplacianSmoothing:
    # type=float|default=0.025: apply Laplacian smoothing
    timer:
    # type=bool|default=False: show timing
    recomputeNormals:
    # type=bool|default=False: recompute normals at each iteration
    normalSmoother:
    # type=float|default=0.2: strength of normal smoother.
    tangentSmoother:
    # type=float|default=0.0: strength of tangential smoother.
    args:
    # type=str|default='': Additional parameters to the command
    environ:
    # type=dict|default={}: Environment variables
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
doctests: []
