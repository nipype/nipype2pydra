# This file is used to manually specify the semi-automatic conversion of
# 'nipype.interfaces.spm.preprocess.Normalize' from Nipype to Pydra.
#
# Please fill-in/edit the fields below where appropriate
#
# Docs
# ----
# use spm_normalise for warping an image to a template
# 
#     http://www.fil.ion.ucl.ac.uk/spm/doc/manual.pdf#page=203
# 
#     Examples
#     --------
#     >>> import nipype.interfaces.spm as spm
#     >>> norm = spm.Normalize()
#     >>> norm.inputs.source = 'functional.nii'
#     >>> norm.run() # doctest: +SKIP
# 
#     
task_name: Normalize
nipype_name: Normalize
nipype_module: nipype.interfaces.spm.preprocess
inputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    template: generic/file
    # type=file|default=<undefined>: template file to normalize to
    parameter_file: generic/file
    # type=file|default=<undefined>: normalization parameter file*_sn.mat
    source_weight: generic/file
    # type=file|default=<undefined>: name of weighting image for source
    template_weight: generic/file
    # type=file|default=<undefined>: name of weighting image for template
    paths: generic/directory+list-of
    # type=inputmultiobject|default=[]: Paths to add to matlabpath
  metadata:
  # dict[str, dict[str, any]] - additional metadata to set on any of the input fields (e.g. out_file: position: 1)
outputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
  callables:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set to the `callable` attribute of output fields
  templates:
  # dict[str, str] - `output_file_template` values to be provided to output fields
  requirements:
  # dict[str, list[str]] - input fields that are required to be provided for the output field to be present
tests:
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    template:
    # type=file|default=<undefined>: template file to normalize to
    source:
    # type=inputmultiobject|default=[]: file to normalize to template
    jobtype:
    # type=enum|default='estwrite'|allowed['est','estwrite','write']: Estimate, Write or do both
    apply_to_files:
    # type=inputmultiobject|default=[]: files to apply transformation to
    parameter_file:
    # type=file|default=<undefined>: normalization parameter file*_sn.mat
    source_weight:
    # type=file|default=<undefined>: name of weighting image for source
    template_weight:
    # type=file|default=<undefined>: name of weighting image for template
    source_image_smoothing:
    # type=float|default=0.0: source smoothing
    template_image_smoothing:
    # type=float|default=0.0: template smoothing
    affine_regularization_type:
    # type=enum|default='mni'|allowed['mni','none','size']: mni, size, none
    DCT_period_cutoff:
    # type=float|default=0.0: Cutoff of for DCT bases
    nonlinear_iterations:
    # type=int|default=0: Number of iterations of nonlinear warping
    nonlinear_regularization:
    # type=float|default=0.0: the amount of the regularization for the nonlinear part of the normalization
    write_preserve:
    # type=bool|default=False: True/False warped images are modulated
    write_bounding_box:
    # type=list|default=[]: 3x2-element list of lists
    write_voxel_sizes:
    # type=list|default=[]: 3-element list
    write_interp:
    # type=range|default=0: degree of b-spline used for interpolation
    write_wrap:
    # type=list|default=[]: Check if interpolation should wrap in [x,y,z] - list of bools
    out_prefix:
    # type=string|default='w': normalized output prefix
    matlab_cmd:
    # type=str|default='': matlab command to use
    paths:
    # type=inputmultiobject|default=[]: Paths to add to matlabpath
    mfile:
    # type=bool|default=True: Run m-code using m-file
    use_mcr:
    # type=bool|default=False: Run m-code using SPM MCR
    use_v8struct:
    # type=bool|default=True: Generate SPM8 and higher compatible jobs
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
doctests: []
