# This file is used to manually specify the semi-automatic conversion of
# 'nipype.interfaces.minc.minc.Extract' from Nipype to Pydra.
#
# Please fill-in/edit the fields below where appropriate
#
# Docs
# ----
# Dump a hyperslab of MINC file data.
# 
#     Examples
#     --------
# 
#     >>> from nipype.interfaces.minc import Extract
#     >>> from nipype.interfaces.minc.testdata import minc2Dfile
# 
#     >>> extract = Extract(input_file=minc2Dfile)
#     >>> extract.run() # doctest: +SKIP
# 
#     >>> extract = Extract(input_file=minc2Dfile, start=[3, 10, 5], count=[4, 4, 4]) # extract a 4x4x4 slab at offset [3, 10, 5]
#     >>> extract.run() # doctest: +SKIP
#     
task_name: Extract
nipype_name: Extract
nipype_module: nipype.interfaces.minc.minc
inputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    input_file: generic/file
    # type=file|default=<undefined>: input file
    output_file: generic/file
    # type=file: output file in raw/text format
    # type=file|default=<undefined>: output file
  metadata:
  # dict[str, dict[str, any]] - additional metadata to set on any of the input fields (e.g. out_file: position: 1)
outputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    output_file: generic/file
    # type=file: output file in raw/text format
    # type=file|default=<undefined>: output file
  callables:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set to the `callable` attribute of output fields
  templates:
  # dict[str, str] - `output_file_template` values to be provided to output fields
    out_file: out_file
    # type=file|default=<undefined>: 
  requirements:
  # dict[str, list[str]] - input fields that are required to be provided for the output field to be present
tests:
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    input_file:
    # type=file|default=<undefined>: input file
    output_file:
    # type=file: output file in raw/text format
    # type=file|default=<undefined>: output file
    write_ascii:
    # type=bool|default=False: Write out data as ascii strings (default).
    write_byte:
    # type=bool|default=False: Write out data as bytes.
    write_short:
    # type=bool|default=False: Write out data as short integers.
    write_int:
    # type=bool|default=False: Write out data as 32-bit integers.
    write_long:
    # type=bool|default=False: Superseded by write_int.
    write_float:
    # type=bool|default=False: Write out data as single precision floating-point values.
    write_double:
    # type=bool|default=False: Write out data as double precision floating-point values.
    write_signed:
    # type=bool|default=False: Write out signed data.
    write_unsigned:
    # type=bool|default=False: Write out unsigned data.
    write_range:
    # type=tuple|default=(0.0, 0.0): Specify the range of output values Default value: 1.79769e+308 1.79769e+308.
    normalize:
    # type=bool|default=False: Normalize integer pixel values to file max and min.
    nonormalize:
    # type=bool|default=False: Turn off pixel normalization.
    image_range:
    # type=tuple|default=(0.0, 0.0): Specify the range of real image values for normalization.
    image_minimum:
    # type=float|default=0.0: Specify the minimum real image value for normalization.Default value: 1.79769e+308.
    image_maximum:
    # type=float|default=0.0: Specify the maximum real image value for normalization.Default value: 1.79769e+308.
    start:
    # type=inputmultiobject|default=[]: Specifies corner of hyperslab (C conventions for indices).
    count:
    # type=inputmultiobject|default=[]: Specifies edge lengths of hyperslab to read.
    flip_positive_direction:
    # type=bool|default=False: Flip images to always have positive direction.
    flip_negative_direction:
    # type=bool|default=False: Flip images to always have negative direction.
    flip_any_direction:
    # type=bool|default=False: Do not flip images (Default).
    flip_x_positive:
    # type=bool|default=False: Flip images to give positive xspace:step value (left-to-right).
    flip_x_negative:
    # type=bool|default=False: Flip images to give negative xspace:step value (right-to-left).
    flip_x_any:
    # type=bool|default=False: Don't flip images along x-axis (default).
    flip_y_positive:
    # type=bool|default=False: Flip images to give positive yspace:step value (post-to-ant).
    flip_y_negative:
    # type=bool|default=False: Flip images to give negative yspace:step value (ant-to-post).
    flip_y_any:
    # type=bool|default=False: Don't flip images along y-axis (default).
    flip_z_positive:
    # type=bool|default=False: Flip images to give positive zspace:step value (inf-to-sup).
    flip_z_negative:
    # type=bool|default=False: Flip images to give negative zspace:step value (sup-to-inf).
    flip_z_any:
    # type=bool|default=False: Don't flip images along z-axis (default).
    out_file:
    # type=file|default=<undefined>: 
    args:
    # type=str|default='': Additional parameters to the command
    environ:
    # type=dict|default={}: Environment variables
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
doctests: []
