# This file is used to manually specify the semi-automatic conversion of
# 'nipype.interfaces.niftyseg.maths.TupleMaths' from Nipype to Pydra.
#
# Please fill-in/edit the fields below where appropriate
#
# Docs
# ----
# Mathematical operations on tuples.
# 
#     See Also
#     --------
#     `Source code <http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftySeg>`__ --
#     `Documentation <http://cmictig.cs.ucl.ac.uk/wiki/index.php/NiftySeg_documentation>`__
# 
#     Examples
#     --------
#     >>> import copy
#     >>> from nipype.interfaces import niftyseg
#     >>> tuple = niftyseg.TupleMaths()
#     >>> tuple.inputs.in_file = 'im1.nii'
#     >>> tuple.inputs.output_datatype = 'float'
# 
#     >>> # Test lncc operation
#     >>> tuple_lncc = copy.deepcopy(tuple)
#     >>> tuple_lncc.inputs.operation = 'lncc'
#     >>> tuple_lncc.inputs.operand_file1 = 'im2.nii'
#     >>> tuple_lncc.inputs.operand_value2 = 2.0
#     >>> tuple_lncc.cmdline
#     'seg_maths im1.nii -lncc im2.nii 2.00000000 -odt float im1_lncc.nii'
#     >>> tuple_lncc.run()  # doctest: +SKIP
# 
#     >>> # Test lssd operation
#     >>> tuple_lssd = copy.deepcopy(tuple)
#     >>> tuple_lssd.inputs.operation = 'lssd'
#     >>> tuple_lssd.inputs.operand_file1 = 'im2.nii'
#     >>> tuple_lssd.inputs.operand_value2 = 1.0
#     >>> tuple_lssd.cmdline
#     'seg_maths im1.nii -lssd im2.nii 1.00000000 -odt float im1_lssd.nii'
#     >>> tuple_lssd.run()  # doctest: +SKIP
# 
#     >>> # Test lltsnorm operation
#     >>> tuple_lltsnorm = copy.deepcopy(tuple)
#     >>> tuple_lltsnorm.inputs.operation = 'lltsnorm'
#     >>> tuple_lltsnorm.inputs.operand_file1 = 'im2.nii'
#     >>> tuple_lltsnorm.inputs.operand_value2 = 0.01
#     >>> tuple_lltsnorm.cmdline
#     'seg_maths im1.nii -lltsnorm im2.nii 0.01000000 -odt float im1_lltsnorm.nii'
#     >>> tuple_lltsnorm.run()  # doctest: +SKIP
# 
#     
task_name: TupleMaths
nipype_name: TupleMaths
nipype_module: nipype.interfaces.niftyseg.maths
inputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    operand_file1: medimage/nifti1
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_file2: generic/file
    # type=file|default=<undefined>: image to perform operation 2 with
    in_file: medimage/nifti1
    # type=file|default=<undefined>: image to operate on
    out_file: generic/file
    # type=file: image written after calculations
    # type=file|default=<undefined>: image to write
  metadata:
  # dict[str, dict[str, any]] - additional metadata to set on any of the input fields (e.g. out_file: position: 1)
outputs:
  omit:
  # list[str] - fields to omit from the Pydra interface
  rename:
  # dict[str, str] - fields to rename in the Pydra interface
  types:
  # dict[str, type] - override inferred types (use "mime-like" string for file-format types,
  # e.g. 'medimage/nifti-gz'). For most fields the type will be correctly inferred
  # from the nipype interface, but you may want to be more specific, particularly
  # for file types, where specifying the format also specifies the file that will be
  # passed to the field in the automatically generated unittests.
    out_file: generic/file
    # type=file: image written after calculations
    # type=file|default=<undefined>: image to write
  callables:
  # dict[str, str] - names of methods/callable classes defined in the adjacent `*_callables.py`
  # to set to the `callable` attribute of output fields
  templates:
  # dict[str, str] - `output_file_template` values to be provided to output fields
  requirements:
  # dict[str, list[str]] - input fields that are required to be provided for the output field to be present
tests:
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    operation:
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value1:
    # type=float|default=0.0: float value to perform operation 1 with
    operand_file2:
    # type=file|default=<undefined>: image to perform operation 2 with
    operand_value2:
    # type=float|default=0.0: float value to perform operation 2 with
    in_file:
    # type=file|default=<undefined>: image to operate on
    out_file:
    # type=file: image written after calculations
    # type=file|default=<undefined>: image to write
    output_datatype:
    # type=enum|default='float'|allowed['char','double','float','input','int','short']: datatype to use for output (default uses input type)
    args:
    # type=str|default='': Additional parameters to the command
    environ:
    # type=dict|default={}: Environment variables
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    in_file:
    # type=file|default=<undefined>: image to operate on
    output_datatype: '"float"'
    # type=enum|default='float'|allowed['char','double','float','input','int','short']: datatype to use for output (default uses input type)
    operation: '"lncc"'
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value2: '2.0'
    # type=float|default=0.0: float value to perform operation 2 with
  imports: &id001
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  - module: copy
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    operation: '"lssd"'
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value2: '1.0'
    # type=float|default=0.0: float value to perform operation 2 with
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
- inputs:
  # dict[str, str] - values to provide to inputs fields in the task initialisation
  # (if not specified, will try to choose a sensible value)
    operation: '"lltsnorm"'
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value2: '0.01'
    # type=float|default=0.0: float value to perform operation 2 with
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  expected_outputs:
  # dict[str, str] - expected values for selected outputs, noting that tests will typically
  # be terminated before they complete for time-saving reasons, and therefore
  # these values will be ignored, when running in CI
  timeout: 10
  # int - the value to set for the timeout in the generated test, 
  # after which the test will be considered to have been initialised 
  # successfully. Set to 0 to disable the timeout (warning, this could
  # lead to the unittests taking a very long time to complete)
  xfail: true
  # bool - whether the unittest is expected to fail or not. Set to false
  # when you are satisfied with the edits you have made to this file
doctests:
- cmdline: seg_maths im1.nii -lncc im2.nii 2.00000000 -odt float im1_lncc.nii
  # str - the expected cmdline output
  inputs:
  # dict[str, str] - name-value pairs for inputs to be provided to the doctest.
  # If the field is of file-format type and the value is None, then the
  # '.mock()' method of the corresponding class is used instead.
    in_file:
    # type=file|default=<undefined>: image to operate on
    output_datatype: '"float"'
    # type=enum|default='float'|allowed['char','double','float','input','int','short']: datatype to use for output (default uses input type)
    operation: '"lncc"'
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value2: '2.0'
    # type=float|default=0.0: float value to perform operation 2 with
  imports: *id001
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  directive:
  # str - any doctest directive to place on the cmdline call, e.g. # doctest: +ELLIPSIS
- cmdline: seg_maths im1.nii -lssd im2.nii 1.00000000 -odt float im1_lssd.nii
  # str - the expected cmdline output
  inputs:
  # dict[str, str] - name-value pairs for inputs to be provided to the doctest.
  # If the field is of file-format type and the value is None, then the
  # '.mock()' method of the corresponding class is used instead.
    operation: '"lssd"'
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value2: '1.0'
    # type=float|default=0.0: float value to perform operation 2 with
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  directive:
  # str - any doctest directive to place on the cmdline call, e.g. # doctest: +ELLIPSIS
- cmdline: seg_maths im1.nii -lltsnorm im2.nii 0.01000000 -odt float im1_lltsnorm.nii
  # str - the expected cmdline output
  inputs:
  # dict[str, str] - name-value pairs for inputs to be provided to the doctest.
  # If the field is of file-format type and the value is None, then the
  # '.mock()' method of the corresponding class is used instead.
    operation: '"lltsnorm"'
    # type=enum|default='lncc'|allowed['lltsnorm','lncc','lssd']: Operation to perform:      * lncc <file> <std> Local CC between current img and <file> on a kernel with <std>     * lssd <file> <std> Local SSD between current img and <file> on a kernel with <std>     * lltsnorm <file_norm> <float>  Linear LTS normalisation assuming <float> percent outliers  
    operand_file1:
    # type=file|default=<undefined>: image to perform operation 1 with
    operand_value2: '0.01'
    # type=float|default=0.0: float value to perform operation 2 with
  imports:
  # list[nipype2pydra.task.base.importstatement] - list import statements required by the test, with each list item
  # consisting of 'module', 'name', and optionally 'alias' keys
  directive:
  # str - any doctest directive to place on the cmdline call, e.g. # doctest: +ELLIPSIS
